# NPM Configuration for @ai-marketplace/sdk
# This ensures proper publishing to npm registry

# Prevent package-lock.json from being created (optional for libraries)
package-lock=false

# Use the official npm registry
registry=https://registry.npmjs.org/

# Use npm for package resolution
prefer-online=true

# Set strict SSL for security
strict-ssl=true

# Configure specific settings for publishing
access=public